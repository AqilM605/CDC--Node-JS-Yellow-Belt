"use strict";
class Todo {
    constructor(id, description, deadline, done) {
        this.id = id,
            this.description = description,
            this.deadline = deadline,
            this.done = done;
    }
}
